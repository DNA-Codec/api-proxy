# Server Configuration
# PORT=3000

# CORS Configuration
# Comma or semicolon separated list of allowed origins
# ALLOWED_ORIGINS=*

# Logging Configuration
# LOG_LEVEL=info

# Proxy Services Configuration
# Format: "subdomain=url;subdomain2=url2"
# Example: "codec=http://localhost:4000;user=http://localhost:5000"
PROXY_SERVICES=codec=http://localhost:4000;user=http://localhost:5000

# Public Paths Configuration (paths that don't require JWT)
# Format: "subdomain=path1,path2;subdomain2=path3,path4"
# Example: "codec=/api/v1/login,/api/v2/login;user=/api/v1/register"
# PROXY_PUBLIC_PATHS=codec=/api/v1/health;user=/api/v1/register,/api/v1/login

# JWT Configuration
# JWT_ENABLED=true
# JWT_SECRET=your_jwt_secret_key_here
# JWT_COOKIE_NAME=token

# Routing Configuration
# Options: "subdomain" or "path"
ROUTING_DETERMINANT=path
